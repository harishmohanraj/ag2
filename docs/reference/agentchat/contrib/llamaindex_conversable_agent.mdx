---
sidebarTitle: llamaindex_conversable_agent
title: autogen.agentchat.contrib.llamaindex_conversable_agent
---

    <h2 id="autogen.agentchat.contrib.llamaindex_conversable_agent.LLamaIndexConversableAgent" class="doc doc-heading">
        <code class="doc-symbol doc-symbol-heading doc-symbol-class"></code>
        <span class="doc doc-object-name doc-class-name">LLamaIndexConversableAgent</span>
        <a href="#autogen.agentchat.contrib.llamaindex_conversable_agent.LLamaIndexConversableAgent" class="headerlink" title="Permanent link"></a>
    </h2>

```python
LLamaIndexConversableAgent(
    name: str,
    llama_index_agent: AgentRunner,
    description: str | None = None,
    **kwargs
)
```

    (In preview) A class for generic conversable agents which can be configured as assistant or user proxy.
    
    After receiving each message, the agent will send a reply to the sender unless the msg is a termination msg.
    For example, AssistantAgent and UserProxyAgent are subclasses of this class,
    configured with different default settings.
    
    To modify auto reply, override `generate_reply` method.
    To disable/enable human response in every turn, set `human_input_mode` to "NEVER" or "ALWAYS".
    To modify the way to get human input, override `get_human_input` method.
    To modify the way to execute code blocks, single code block, or function call, override `execute_code_blocks`,
    `run_code`, and `execute_function` methods respectively.

<b>Parameters:</b>
| Name | Description |
|--|--|
| `name` | agent name.<br/><br/>**Type:** `str` |
| `llama_index_agent` | llama index agent.<br/><br/>Please override this attribute if you want to reprogram the agent.<br/><br/>**Type:** `AgentRunner` |
| `description` | a short description of the agent.<br/><br/>This description is used by other agents (e.g.<br/><br/>the GroupChatManager) to decide when to call upon this agent.<br/><br/>**Type:** `str \| None`<br/><br/>**Default:** None |
| `**kwargs` | Please refer to other kwargs in [ConversableAgent](../conversable_agent#init).<br/><br/> |